# `@ryanarora/eslint-config`

> My personal [ESLint](https://eslint.org) config.

## Usage

**Install**:

```bash
$ pnpm add -D @ryanarora/eslint-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "eslintConfig": {
    "extends": "@ryanarora/eslint-config"
  }
}
```
